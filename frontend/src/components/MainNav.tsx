import { Button } from "./ui/button";

const MainNav = () => {
  return (
    <Button className="text-center flex items-center font-bold hover:text-orange-500 pb-0">
      Login
    </Button>
  );
};

export default MainNav;
